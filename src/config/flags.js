// Feature flags — compile-time defaults.
// Runtime overrides are stored in localStorage (see Settings screen).
// Flip a flag here to permanently enable/disable a feature for a build.

export const DEMO_MODE = false;            // show "Kitchen Fire Emergency" demo button
export const FAMILY_NETWORK_ENABLED = false; // phase 3: alert family members via network
